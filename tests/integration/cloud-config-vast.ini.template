# Template for VAST Data integration test credentials.
# Copy to cloud-config-vast.ini and fill in values, or set environment variables.
#
# Environment variables take precedence via lookup('env', ...) in defaults/main.yml.

[vast]
VAST_VMS_HOST=
VAST_VMS_USER=
VAST_VMS_PASSWORD=
