---
- name: Create snapshot
  stevefulme1.vastdata.vast_snapshot:
    vms_host: "{{ vast_vms_host }}"
    vms_user: "{{ vast_vms_user }}"
    vms_password: "{{ vast_vms_password }}"
    name: "{{ vast_test_snapshot_name }}"
    state: present
  register: create_result

- name: Verify creation
  ansible.builtin.assert:
    that:
      - create_result is changed

- name: Create same snapshot again (idempotency)
  stevefulme1.vastdata.vast_snapshot:
    vms_host: "{{ vast_vms_host }}"
    vms_user: "{{ vast_vms_user }}"
    vms_password: "{{ vast_vms_password }}"
    name: "{{ vast_test_snapshot_name }}"
    state: present
  register: idem_result

- name: Verify idempotency
  ansible.builtin.assert:
    that:
      - idem_result is not changed

- name: Delete snapshot
  stevefulme1.vastdata.vast_snapshot:
    vms_host: "{{ vast_vms_host }}"
    vms_user: "{{ vast_vms_user }}"
    vms_password: "{{ vast_vms_password }}"
    name: "{{ vast_test_snapshot_name }}"
    state: absent
  register: delete_result

- name: Verify deletion
  ansible.builtin.assert:
    that:
      - delete_result is changed

- name: Delete again (idempotency)
  stevefulme1.vastdata.vast_snapshot:
    vms_host: "{{ vast_vms_host }}"
    vms_user: "{{ vast_vms_user }}"
    vms_password: "{{ vast_vms_password }}"
    name: "{{ vast_test_snapshot_name }}"
    state: absent
  register: delete_idem

- name: Verify delete idempotency
  ansible.builtin.assert:
    that:
      - delete_idem is not changed
