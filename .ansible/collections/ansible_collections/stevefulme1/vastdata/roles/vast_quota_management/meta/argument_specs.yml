---
argument_specs:
  main:
    short_description: "Configure quota policies"
    options:
      vast_quota_management_host:
        description: Target host address
        type: str
        required: true
      vast_quota_management_username:
        description: Authentication username
        type: str
      vast_quota_management_password:
        description: Authentication password
        type: str
        no_log: true
      vast_quota_management_api_key:
        description: API key for authentication
        type: str
        no_log: true
      vast_quota_management_validate_certs:
        description: Validate SSL certificates
        type: bool
        default: true
      vast_quota_management_state:
        description: Desired state
        type: str
        default: present
        choices: [present, absent]
