---
argument_specs:
  main:
    short_description: "Set up alerting and capacity monitoring"
    options:
      vast_monitoring_host:
        description: Target host address
        type: str
        required: true
      vast_monitoring_username:
        description: Authentication username
        type: str
      vast_monitoring_password:
        description: Authentication password
        type: str
      vast_monitoring_api_key:
        description: API key for authentication
        type: str
      vast_monitoring_validate_certs:
        description: Validate SSL certificates
        type: bool
        default: true
      vast_monitoring_state:
        description: Desired state
        type: str
        default: present
        choices: [present, absent]
